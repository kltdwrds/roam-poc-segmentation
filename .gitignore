# Local Python env / build artifacts
.venv/
__pycache__/
*.pyc
.DS_Store

# Data — never commit (S3DIS preprocessed npys are redownloadable, 8 GB)
data/

# Redownloadable pretrained weights (curl-able, big, not the artifact)
checkpoints/sparseconvunet_scannet.pth
checkpoints/ptv3_s3dis.pth
checkpoints/randlanet_s3dis.pth

# Fine-tuned checkpoints — 192 MB each, over GitHub's 100 MB file limit
# See NOTES.md "Loading the fine-tuned model" for retrain / request instructions
checkpoints/finetune/ptv3_pt_epoch*.pth

# Tensorboard / training run logs (transient, big, not part of the deliverable)
runs/
wandb/
*.log

# Broken-pretrained sanity-check outputs (kept locally for diagnostic record, not shipped)
outputs/day2_pretrained/

# Smoke-test eval (redundant with results.json)
eval/_test.json

# Smoke-test scaffolding
src/_smoke_finetune.py

# Anything in src/__pycache__ etc
**/__pycache__/
